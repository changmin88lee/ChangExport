# 창Export 작업 및 로컬 Git 관리

- 기본 저장소는 이 폴더이며 기본 브랜치는 `main`이다.
- 작업 시작 시 이 문서와 `git status`를 확인하고 기존 사용자 변경을 보존한다.
- 사용자가 질문, 검토 또는 진단만 요청하면 읽기 전용으로 진행한다. UI·로직 변경은 명시적인 변경 요청 범위에서만 수행한다.
- 승인된 변경을 완료하고 검증한 뒤 해당 작업의 파일만 로컬 커밋으로 기록한다. 무관한 사용자 변경을 포함하지 않는다.
- 소스 변경 시 `dotnet build .\ChangExport.csproj --configuration Release`로 검증한다. 빌드 실패나 비밀정보가 있으면 커밋하지 않고 보고한다.
- 소스, 설정 예제, 요구사항 문서와 설치·빌드 스크립트는 추적한다. `bin`, `obj`, `배포폴더`, RVT 및 DWG 산출물은 기본적으로 추적하지 않는다.
- 최초 베타 기준 태그는 `beta-0.1.0`이다. 기존 태그와 커밋을 덮어쓰거나 이력을 재작성하지 않는다.
- 원격 저장소 연결, GitHub 업로드 및 원격 동기화는 별도 사용자 요청이 있을 때만 수행한다.
- 예약 Git 자동화는 별도 요청 없이 생성하지 않는다.
