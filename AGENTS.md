# 창Export 작업 규칙

## 1. 적용 범위와 시작 절차

- 이 규칙은 `C:\Users\user\Desktop\창Export` 저장소에 적용한다. 기본 브랜치는 `main`이다.
- 새 작업 시작 시 이 문서를 끝까지 읽고 현재 브랜치, `git status`, 관련 diff를 확인한다.
- 세부 작업 원칙은 [창Export 필수 작업 규칙](docs/창Export_필수_작업규칙.md), 이력·배포 절차는 [Git 이력관리 규칙](docs/GIT_이력관리_규칙.md)을 따른다. 해당 작업에 필요한 상세 문서는 변경 전에 읽는다.
- 대화가 요약되거나 다른 작업으로 인계되면 승인된 범위와 보류 항목을 다시 확인한다. 기억에 없는 요구사항을 만들어내지 않는다.
- 현재 사용자의 명시적 지시와 변경 금지가 개발계획서의 과거 제안보다 우선한다. MD에 기능이 적혀 있다는 이유만으로 구현하지 않는다.
- 창Rebar·창BIM은 참고 프로젝트다. 그 프로젝트의 경로, 버전 번호, 자동화, 원격 주소와 전용 로직을 창Export에 그대로 적용하지 않는다.

## 2. 최우선: 문제 보고와 수정 승인 분리

- 불편 표현, 오류 보고, 질문, 이미지, 로그와 테스트 실패는 수정 승인이 아니다.
- 수정 요청이 없으면 읽기 전용 분석을 하고 대기한다. UI·코드·설정·문서·버전·모델·배포물을 임의로 바꾸지 않는다.
- `로직 수정하지 마세요`, `검토만`, `답변만`, `일단 구축만`, `대기`라는 지시는 그대로 지킨다. UI 개선을 구실로 변경 금지를 우회하지 않는다.
- UI만 허용되면 분류·수집·저장·출력 로직을 바꾸지 않는다. 문서만 허용되면 소스·설정·DLL을 바꾸지 않는다. 테스트만 허용되면 실패한 코드를 자동 수정하지 않는다.
- 한두 가지 UI·로직 문제의 수정은 원인, 변경 파일, 유지할 동작과 검증 범위를 설명한 뒤 진행 여부를 재확인한다. 해당 범위에 대한 최종 승인이 이미 있거나 사용자가 명시적으로 재확인을 생략하라고 한 경우에는 반복해서 묻지 않는다.
- 명확한 문서 작성·Git 구성 요청은 그 요청 범위의 승인으로 취급한다. 이를 애드인 기능 변경 승인으로 확대하지 않는다.
- 대규모 변경이나 누적 문제는 영향과 단계별 검증을 정리해 제안한다. 제안은 승인이 아니며, 범위가 커지면 다시 확인한다.
- 사용자가 보류·중단하면 멈춘다. 승인되지 않은 개선 후보는 대화에서 관리하고, 파일 기록은 요청된 경우에만 수행한다.

## 3. 승인 후 구현 원칙

- 사용자가 승인한 대상과 제외 대상을 먼저 밝히고 최소 변경으로 처리한다.
- 무관한 리팩터링, 기본값·허용오차 변경, 자동 필터·판정·삭제, 새 라이브러리를 끼워 넣지 않는다.
- 기존 미커밋 변경은 사용자 소유로 보고 보존한다. 겹치는 변경을 안전하게 분리할 수 없으면 확인을 요청한다.
- `README.md`와 원본 개발계획서 두 파일은 해당 문서 수정이 명시적으로 요청된 경우에만 변경한다.
- 실험 기능이나 미구현 버튼을 실제 지원 기능으로 설명하지 않는다. 설정 저장, 판정 미리보기와 최종 DWG 반영을 구분한다.
- 실제 Revit·DWG 테스트를 하지 않았다면 빌드 성공만으로 기능 검증이 끝났다고 말하지 않는다.

## 4. 창Export에 적용할 개발 기준

- CAD 출력을 위해 사용자의 모델, Family, Subcategory, Object Styles와 원본 View를 임의로 바꾸지 않는다.
- 개발 명세상 일반 객체는 Rule 기반 자동 분류 대상이고 `CAD_LAYER` 수동 지정은 예외 처리다. 이 기술 검증 기능을 모든 객체의 필수 사전 작업처럼 만들지 않는다.
- `CAD_LAYER` 저장, Rule 판정, Native Export, 최종 DWG Layer 변환·병합은 서로 다른 단계다. 연결되지 않은 단계는 명확히 알린다.
- 공유 매개변수의 고정 GUID와 인스턴스 Binding을 보존한다. 임시 Shared Parameter 경로와 임시 View·Filter는 예외·취소 시에도 복구·정리하도록 검증한다.
- 사용자 Profile·원본 RVT·기존 DWG를 자동 덮어쓰지 않는다. SDK 도입과 재배포 조건은 별도 확인한다.
- UI 용어와 조작 방식은 실제 사용 흐름에 맞춰 제안하되, 용어 변경·선택형 입력·레이어 목록 확장도 승인 전에는 구현하지 않는다.
- 현재 베타의 후처리 엔진 미연결 상태를 숨기거나 단일 DWG Model Space 병합이 완료된 것처럼 보고하지 않는다.

## 5. 검증과 배포

- 분석만 한 작업에는 빌드·패키징·Git 쓰기를 하지 않는다. 사용자가 테스트 실행을 요청한 경우에는 요청 범위의 검증만 수행한다.
- 승인된 문서 변경만 있으면 문서·경로·diff를 검사하고 DLL 빌드와 최신 배포 폴더 갱신은 하지 않는다.
- 코드 또는 DLL에 영향을 주는 프로젝트 설정 변경은 관련 테스트와 `dotnet build .\ChangExport.csproj --configuration Release`로 검증한다.
- 코드 변경 후 검증된 최신 배포본 갱신에는 기존 `Build-Latest.ps1`을 사용한다. `-SkipBuild`는 동일 소스의 Release 빌드 성공을 확인한 경우에만 사용한다.
- 빌드 DLL과 `배포폴더\최신\ChangExport.dll`의 크기·SHA-256 일치를 확인한다. 불일치나 검증 실패 상태를 배포 완료로 보고하지 않는다.
- 설치 안내는 `C:\Users\user\Desktop\창Export\배포폴더\최신\설치.cmd`로 통일한다. 실제 설치·제거·Revit 재시작은 별도 사용자 요청 없이 실행하지 않는다.
- ZIP은 요청된 배포 작업에만 생성한다. 기존 역사 ZIP은 덮어쓰지 않으며, 설치 스크립트가 참조하는 ZIP 내부 DLL까지 검증한다.

## 6. 로컬 Git 이력

- 승인된 작업을 검증한 뒤 해당 파일만 로컬 커밋한다. 분석만 한 작업, 검증 실패, 비밀정보, 충돌 또는 변경 없는 상태에서는 커밋하지 않는다.
- 기존 베타 기준 `beta-0.1.0`은 보존한다. 이후 코드·문서·설정 작업은 `V1`, `V2`, … 작업 이력 번호로 기록한다.
- 커밋 직전에 가장 큰 숫자형 `V###` 태그를 확인한다. 제목은 `V### 변경 요약`, 태그는 같은 번호의 주석 태그로 만든다.
- Git 작업 번호와 제품 버전은 구분한다. 문서만 변경한 작업에서는 제품 `VERSION` 및 DLL 버전을 올리지 않는다.
- `bin`, `obj`, `배포폴더`, RVT와 DWG는 기존 `.gitignore`대로 제외한다. 이력을 위해 별도 V번호 폴더나 소스 복사본을 만들지 않는다.
- 커밋 amend, 태그 강제 이동, 강제 push와 사용자 변경의 임의 삭제·롤백을 하지 않는다.
- 원격 연결 및 `fetch`, `pull`, `push`, `ls-remote` 등 네트워크 Git 명령은 명시적인 원격 동기화 요청이 있을 때만 수행한다.
- 예약 자동화는 만들거나 재활성화하지 않는다. 현재 작업 중 완료된 변경의 로컬 커밋과 예약 실행은 구분한다.
- 완료 시 변경 범위, 검증 결과, 커밋·태그와 남은 실제 테스트를 간결하게 보고한다.
